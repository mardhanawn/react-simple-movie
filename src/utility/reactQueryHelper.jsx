export const defaultQueryOption = {
    retry: false,
    refetchOnWindowFocus: false,
    // retry: ({ response, ...rest }) => {
    //     if (response === undefined) {
    //         console.error(rest)
    //         return false
    //     }
    // },
}
